<div class="form">

    <form action="/api/getTrainer">
        <input id="searchfield" type="text" class="find" name="query" placeholder="Search..">
        <input type="submit" onsubmit=searchUser()>
    </form>

    <table>
        <tr>
            <th>Username</th>
            <th>Name</th>
            <th>Date of Birth</th>
            <th>Email</th>
            <th>Telephone</th>
            <th>Education</th>
            <th>Bio</th>
            <th>Course</th>
            <th>Update</th>
            <th>Delete</th>
        </tr>
        {{#each user}}
            <tr>
                <th>{{this.username}}</th>
                <th>{{this.name}}</th>
                <th>{{this.dob}}</th>
                <th>{{this.email}}</th>
                <th>{{this.telephone}}</th>
                <th>{{this.education}}</th>
                <th>{{this.bio}}</th>
                <th>
                    {{#each courseAssign}}
                        {{this.name}} <br>
                    {{/each}}
                </th>
                <th>
                    <a class="btn add" href='/api/updateInfo?user_id={{_id}}'>Update</a>
                </th>
                <th>
                    <a class="btn add" onclick="onDelete('{{_id}}')">Delete</a>
                </th>
            </tr>
        {{/each}}
    </table>
</div>

<script>
    function onDelete(user_id) {
        try {
            const myHeaders = new Headers();
            myHeaders.append("Content-Type", "application/json");

            const raw = JSON.stringify({
                "delete_id": user_id
            });

            const requestOptions = {
                method: 'POST',
                headers: myHeaders,
                body: raw,
                redirect: 'follow'
            };

            fetch("/staff/deleteAccount", requestOptions)
                    .then(response => response.text())
                    .then(result => {
                        window.location.reload();
                        console.log(result)
                    })
                    .catch(error => console.log('error', error));
        } catch (e) {
            console.log(e);
        }
    }
</script>